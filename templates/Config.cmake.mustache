# Auto-generated by mimi-pkg. Do not edit by hand.
# Package: {{packageName}} Version: {{version}} Namespace: {{namespace}}

# Compute bundle roots relative to this file so the package is relocatable.
get_filename_component(_{{packageVar}}_cmakedir "${CMAKE_CURRENT_LIST_FILE}" PATH)
get_filename_component(_{{packageVar}}_root "${_{{packageVar}}_cmakedir}/.." ABSOLUTE)
set(_{{packageVar}}_includedir "${_{{packageVar}}_root}/include")
set(_{{packageVar}}_libdir     "${_{{packageVar}}_root}/libs")
set(_{{packageVar}}_bindir     "${_{{packageVar}}_root}/bins")

# Exported include dirs (package-level)
set({{packageName}}_INCLUDE_DIRS "${_{{packageVar}}_includedir}"{{#extraIncludeDirs}} "{{.}}"{{/extraIncludeDirs}})
set({{packageName}}_LIB_DIR      "${_{{packageVar}}_libdir}")
set({{packageName}}_BIN_DIR      "${_{{packageVar}}_bindir}")

{{#libs}}
################################################################################
# Target: {{targetName}} ({{type}})
################################################################################

add_library({{targetName}} {{type}} IMPORTED GLOBAL)

{{#locations.DEBUG}}
# Debug config
set_property(TARGET {{targetName}} PROPERTY IMPORTED_CONFIGURATIONS DEBUG)
{{/locations.DEBUG}}
{{#locations.RELEASE}}
# Release config
set_property(TARGET {{targetName}} APPEND PROPERTY IMPORTED_CONFIGURATIONS RELEASE)
{{/locations.RELEASE}}
{{#locations.SINGLE}}
# Single config
set_target_properties({{targetName}} PROPERTIES
  IMPORTED_LOCATION "${_{{packageVar}}_libdir}/{{{locations.SINGLE.binary}}}"
{{#locations.SINGLE.implib}}
  IMPORTED_IMPLIB "${_{{packageVar}}_libdir}/{{{locations.SINGLE.implib}}}"
{{/locations.SINGLE.implib}}
)
{{/locations.SINGLE}}
{{#locations.DEBUG}}
{{#locations.RELEASE}}
set_target_properties({{targetName}} PROPERTIES
{{#locations.DEBUG.binary}}
  IMPORTED_LOCATION_DEBUG "${_{{packageVar}}_libdir}/{{{locations.DEBUG.binary}}}"
{{/locations.DEBUG.binary}}
{{#locations.RELEASE.binary}}
  IMPORTED_LOCATION_RELEASE "${_{{packageVar}}_libdir}/{{{locations.RELEASE.binary}}}"
{{/locations.RELEASE.binary}}
{{#locations.DEBUG.implib}}
  IMPORTED_IMPLIB_DEBUG "${_{{packageVar}}_libdir}/{{{locations.DEBUG.implib}}}"
{{/locations.DEBUG.implib}}
{{#locations.RELEASE.implib}}
  IMPORTED_IMPLIB_RELEASE "${_{{packageVar}}_libdir}/{{{locations.RELEASE.implib}}}"
{{/locations.RELEASE.implib}}
)
{{/locations.RELEASE}}
{{^locations.RELEASE}}
set_target_properties({{targetName}} PROPERTIES
{{#locations.DEBUG.binary}}
  IMPORTED_LOCATION_DEBUG "${_{{packageVar}}_libdir}/{{{locations.DEBUG.binary}}}"
{{/locations.DEBUG.binary}}
{{#locations.DEBUG.implib}}
  IMPORTED_IMPLIB_DEBUG "${_{{packageVar}}_libdir}/{{{locations.DEBUG.implib}}}"
{{/locations.DEBUG.implib}}
)
{{/locations.RELEASE}}
{{/locations.DEBUG}}
{{^locations.DEBUG}}
{{#locations.RELEASE}}
set_target_properties({{targetName}} PROPERTIES
{{#locations.RELEASE.binary}}
  IMPORTED_LOCATION_RELEASE "${_{{packageVar}}_libdir}/{{{locations.RELEASE.binary}}}"
{{/locations.RELEASE.binary}}
{{#locations.RELEASE.implib}}
  IMPORTED_IMPLIB_RELEASE "${_{{packageVar}}_libdir}/{{{locations.RELEASE.implib}}}"
{{/locations.RELEASE.implib}}
)
{{/locations.RELEASE}}
{{/locations.DEBUG}}
{{#linkLibraries.length}}
# Transitive link interface
set_property(TARGET {{targetName}} PROPERTY
  INTERFACE_LINK_LIBRARIES
    {{#linkLibraries}}{{{.}}}{{/linkLibraries}}
)
{{/linkLibraries.length}}
{{#compileDefinitions.length}}
# Optional compile defs / opts
set_property(TARGET {{targetName}} PROPERTY
  INTERFACE_COMPILE_DEFINITIONS {{#compileDefinitions}}{{.}};{{/compileDefinitions}}
)
{{/compileDefinitions.length}}
{{#compileOptions.length}}
set_property(TARGET {{targetName}} PROPERTY
  INTERFACE_COMPILE_OPTIONS {{#compileOptions}}{{.}};{{/compileOptions}}
)
{{/compileOptions.length}}
{{/libs}}

{{#interfaceTargetName}}
{{#libs.length}}
################################################################################
# Interface Target: {{interfaceTargetName}}
################################################################################

add_library({{interfaceTargetName}} INTERFACE IMPORTED GLOBAL)

# Common interface includes
set_property(TARGET {{interfaceTargetName}} PROPERTY
  INTERFACE_INCLUDE_DIRECTORIES
    "${_{{packageVar}}_includedir}"{{#extraIncludeDirs}}
    "{{.}}"{{/extraIncludeDirs}}
)

# Link all individual library targets
set(_{{packageVar}}_interface_libs {{#libs}}{{targetName}}{{^last}};{{/last}}{{/libs}})
set_property(TARGET {{interfaceTargetName}} PROPERTY
  INTERFACE_LINK_LIBRARIES
    ${_{{packageVar}}_interface_libs}
)
{{/libs.length}}
{{/interfaceTargetName}}

################################################################################
# Back-compat variables for simple consumers
################################################################################
set({{packageName}}_FOUND TRUE)

################################################################################
# Tidy
################################################################################
unset(_{{packageVar}}_cmakedir)
unset(_{{packageVar}}_root)
unset(_{{packageVar}}_includedir)
unset(_{{packageVar}}_libdir)
unset(_{{packageVar}}_bindir)
unset(_{{packageVar}}_interface_libs)